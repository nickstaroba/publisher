import React from "react";

import { Breakpoints as Component } from "./Breakpoints";

export default {
  title: "Breakpoints",
  component: Component,
};

const Template = (args) => <Component {...args} />;

export const Breakpoints = Template.bind({});

Breakpoints.args = {};
