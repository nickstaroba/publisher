import React from "react";

import { Fonts as Component } from "./Fonts";

export default {
  title: "Fonts",
  component: Component,
};

const Template = (args) => <Component {...args} />;

export const Fonts = Template.bind({});

Fonts.args = {};
